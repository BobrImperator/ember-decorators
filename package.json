{
  "name": "@ember-decorators/ember-decorators",
  "private": true,
  "description": "Core library for Ember Decorators",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r build && pnpm --filter ember-decorators test",
    "start": "pnpm --filter ember-decorators start"
  },
  "engines": {
    "node": ">= 18"
  }
}
